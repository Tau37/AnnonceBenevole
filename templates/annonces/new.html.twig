{% extends 'base.html.twig' %}

{% block title %}Déposez votre annonce{% endblock %}

{% block body %}
    <div class="container">
      <div class="container d-flex align-items-center justify-content-between">
      <h1 class="mt-4 mb-4">Déposez votre annonce 👍</h1>
      {{ form_start(form)}}
      {{ form_row(form.linkCategorie, {'label' : 'Choisissez votre catégorie'}) }}
    </div>
      <div class="form-group">
              {{ form_label(form.annonces, 'Titre de votre annonce')}}
              {{ form_widget(form.annonces, {'attr':{'class':'form-control'}})}}
          </div>
      <div class="form-group">
        {{ form_widget(form.description, { 'attr': {'class': 'form-control','placeholder': 'Ecrivez votre annonce', 'style': 'height:55vh' } }) }}
      </div>
      <button type="submit" class="btn btn-primary">Envoyer</button>
      {{ form_end(form) }}
      <div class="mt-3">
        <a href="{{ path('list') }}">Mes annonces visible</a> {# A modifier #}
      </div>
    </div>
{% endblock %}
