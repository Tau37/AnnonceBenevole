{% extends 'base.html.twig' %}

{% block title %}Home
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1>Les annonces 👍 </h1>
	</div>

<section id="annonces">
<div>
<img src={{ asset("assets/img/categorie/{{item.linkCategorie.id}}.jpg") }} alt="">

</div>
{% for item in annonces %}
    
    {# <pre>
		{{ dump(item) }}
	</pre> #}
	<div class="card card-centrer">
		<img class="card-img-top" src="#" alt="#">
		<div class="card-body">
			<h5 class="card-title">{{ item.annonces }}</h5>
			<p class="card-title">De : {{ item.linkAnnonce.prenom }}</p>
			<p class="card-text">posté le : {{ item.date ? item.date|date('Y-m-d à H') : '' }} heures </p>
			<p class="card-text">{{ item.description }}</p>
			<p class="card-text">{{ item.linkCategorie.categorie }}</p>
			<p class="card-text">{{ item.linkCategorie.id }}</p>
			<img src="../../assets/img/categorie/{{item.linkCategorie.id}}.jpg" alt="">
			<a href="#" class="btn btn-primary">Plus d'info... (à faire)</a>
			{# <a href="registration.php" class="btn btn-primary">Inscrivez-vous !</a> #}
		</div>
	</div>

{% endfor %}
</section>


{% endblock %}
